{
  "name": "predix-contracts",
  "version": "1.0.0",
  "description": "PredictionMarket smart contracts — zero custody, multi-role security",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "test:coverage": "hardhat coverage",
    "deploy:testnet": "hardhat run script/deploy.js --network amoy",
    "deploy:mainnet": "hardhat run script/deploy.js --network polygon",
    "verify:testnet": "hardhat verify --network amoy",
    "verify:mainnet": "hardhat verify --network polygon",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.0.0",
    "hardhat": "^2.22.0"
  }
}
